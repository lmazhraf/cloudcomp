name: Deploy to VPS

on:
  push:
    branches: [ "Main" ]  
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            cd /var/www/taskplanner
            git pull origin Main
            chown -R www-data:www-data /var/www/taskplanner
            # Jika perlu restart layanan, aktifkan baris di bawah
            # systemctl reload nginx